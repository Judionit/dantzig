function minimisation() {
   let i = 1;
   let k = 0;
   let n = 16;
   let x; 
   x > circle.parentNode;
   let x1; 
   x1 > cirle.lastChild;
   let lambda = v(xi,xi);
   while (i < n) {
       if(i === 1) {
           lambda1 = 0;
       }
       else {
           let lambdai = lambdap + v(xp, xp);
           lambdai = min[lambdai + lambda];
           lambdai = min[xi + lambda];
       }
   }
   i++;
   k++;
}